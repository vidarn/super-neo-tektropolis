#ifndef COMMON_N9GPK5W1
#define COMMON_N9GPK5W1

#define PIXELS_PER_METER 50.0f;
#define METER_PER_PIXELS 0.02f;

float screenToWorld(int  val);
int worldToScreen(float val);

#endif /* end of include guard: COMMON_N9GPK5W1 */
